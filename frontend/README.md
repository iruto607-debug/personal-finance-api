# Frontend demo

This directory contains a minimal static demo page. Serve it with a simple static server while the backend runs.

Quick serve command (Python 3):

```bash
cd frontend
python -m http.server 8080
# then open http://localhost:8080
```
